# Optional LLM ranking: enable with python run.py --llm.
# .env is not loaded automatically; use process environment variables.
# Supply real values through the process environment or CI secrets, never Git.
# OPENAI_MODEL optionally overrides the model documented in README.
OPENAI_API_KEY=
